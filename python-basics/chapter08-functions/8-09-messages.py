messages = ['sup', 'yo', 'hey', 'howdy']

def show_message(message):
    """this function takes a list as input and displays all items in the list"""
    for message in messages:
        print(message)

show_message(messages)
